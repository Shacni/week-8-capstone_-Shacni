
import PageTemplate from "@/components/PageTemplate";

const Settings = () => {
  return (
    <PageTemplate 
      title="System Settings" 
      description="Configure system preferences, security settings, and administrative options"
    />
  );
};

export default Settings;
