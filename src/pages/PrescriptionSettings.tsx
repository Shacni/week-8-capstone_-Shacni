
import PageTemplate from "@/components/PageTemplate";

const PrescriptionSettings = () => {
  return (
    <PageTemplate 
      title="Prescription Settings" 
      description="Configure dosage, frequency, routes, and medication settings"
    />
  );
};

export default PrescriptionSettings;
