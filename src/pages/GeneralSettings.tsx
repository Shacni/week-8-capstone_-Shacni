
import PageTemplate from "@/components/PageTemplate";

const GeneralSettings = () => {
  return (
    <PageTemplate 
      title="General Settings" 
      description="Configure users, roles, departments, and general system settings"
    />
  );
};

export default GeneralSettings;
