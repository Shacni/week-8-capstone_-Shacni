
import PageTemplate from "@/components/PageTemplate";

const Packages = () => {
  return (
    <PageTemplate 
      title="Packages" 
      description="Medical service packages, treatment bundles, and pricing plans"
    />
  );
};

export default Packages;
